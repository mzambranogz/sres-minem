@using sres.app.Models
@using sres.be
@using sres.ut
@{
    ViewBag.Titulo = "Bandeja";
    ViewBag.SRESDescription = false;
    UsuarioBE usuario = Session["user"] == null ? null : (UsuarioBE)Session["user"];
    //ViewBag.convocatoria = !ViewData.ContainsKey("convocatoria") ? null : ViewData["convocatoria"] == null ? null : (ConvocatoriaBE)ViewData["convocatoria"];
}
@section scripts_footer{
    <script>
        const maxBytes = @(AppSettings.Get<long>("File.MaxBytes"));
    </script>
    <script src="@Url.Content("~/Assets/js/plugins/alert.js")"></script>
    <script src="@Url.Content("~/Assets/js/pages/convocatoria.js")"></script>
}

@if(usuario.ID_ROL == (int)EnumsCustom.Roles.POSTULANTE)
{
    @Html.Partial("IndexPostulante")
} else {
    @Html.Partial("IndexEvaluador")
}


<script>
    const idSector = @usuario.INSTITUCION.ID_SECTOR;
    const idRol = @usuario.ID_ROL;
    var primerinicio = @usuario.INSTITUCION.PRIMER_INICIO;
    var idDepartamento = '@usuario.INSTITUCION.ID_DEPARTAMENTO';
    var idProvincia = '@usuario.INSTITUCION.ID_PROVINCIA';
    var idDistrito = '@usuario.INSTITUCION.ID_DISTRITO';
    var vidProvincia = '@usuario.INSTITUCION.ID_PROVINCIA';
    var vidDistrito = '@usuario.INSTITUCION.ID_DISTRITO';
    var idSubsectortipoemp = @usuario.INSTITUCION.ID_SUBSECTOR_TIPOEMPRESA;
    var idTrabajadorCama = @usuario.INSTITUCION.ID_TRABAJADORES_CAMA;
    var cantidad = @usuario.INSTITUCION.CANTIDAD;
    var cantidadmujeres = @usuario.INSTITUCION.CANTIDAD_MUJERES;
    var vidTrabajadorCama = @usuario.INSTITUCION.ID_TRABAJADORES_CAMA;
    var vcantidad = @usuario.INSTITUCION.CANTIDAD;
    var vcantidadmujeres = @usuario.INSTITUCION.CANTIDAD_MUJERES;
</script>